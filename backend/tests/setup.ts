// Jest setup file
// Jest provides expect globally, no need to import

// Setup test database or mock services here
// beforeEach and afterEach hooks are available globally in Jest

// You can add global test setup here if needed
// For example: database connection setup, global mocks, etc.